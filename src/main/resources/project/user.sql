create database user;

create table user_data (id int(10) auto_increment primary key, name varchar(20),email_id varchar(40),address varchar(20), phone_no varchar(20), birth_date date);

create database spring_batch;